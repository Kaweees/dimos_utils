package shape_msgs;

struct Mesh {
    int32_t triangles_length;
    int32_t vertices_length;
    shape_msgs.MeshTriangle triangles[triangles_length];
    geometry_msgs.Point vertices[vertices_length];
}
