package sensor_msgs;

struct PointCloud {
    int32_t points_length;
    int32_t channels_length;
    std_msgs.Header header;
    geometry_msgs.Point32 points[points_length];
    sensor_msgs.ChannelFloat32 channels[channels_length];
}
