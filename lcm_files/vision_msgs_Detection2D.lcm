package vision_msgs;

struct Detection2D {
    int32_t results_length;
    std_msgs.Header header;
    vision_msgs.ObjectHypothesisWithPose results[results_length];
    vision_msgs.BoundingBox2D bbox;
    string id;
}
